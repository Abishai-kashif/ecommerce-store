"use client";

function Loading() {
    return (
        <main className="text-center mt-10 h-screen w-screen">
            <h1 className="text-5xl font-bold text-gray-800">Loading....</h1>
        </main>
    );
}

export default Loading;
